#!/usr/bin/env bash

if [ ! -z "${DEV_BASE// }" ]
then
  cd $DEV_BASE/c/xlreg_c
  
  blListGen \
    -d ../xlreg_c \
    -1L  \
    -u $DVCZ_UDIR \
    -X build \
    -X .ssh \
    $@
  
else
  echo "DEV_BASE is not defined"
fi
